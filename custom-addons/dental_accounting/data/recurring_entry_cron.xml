<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron Job for Recurring Payments -->
        <record id="ir_cron_recurring_payments" model="ir.cron">
            <field name="name">Generate Recurring Payment Entries</field>
            <field name="model_id" ref="model_account_recurring_payments"/>
            <field name="state">code</field>
            <field name="code">model._cron_generate_entries()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
        </record>
    </data>
</odoo>

