<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Lock Date Wizard Form -->
    <record id="view_account_lock_date_form" model="ir.ui.view">
        <field name="name">account.lock.date.form</field>
        <field name="model">account.lock.date</field>
        <field name="arch" type="xml">
            <form string="Khóa Kỳ Kế toán">
                <group>
                    <field name="company_id" invisible="1"/>
                    <field name="period_lock_date" string="Ngày khóa kỳ (Không phải Cố vấn)"/>
                    <field name="fiscalyear_lock_date" string="Ngày khóa năm tài chính"/>
                </group>
                <footer>
                    <button string="Cập nhật" name="execute" type="object" class="btn-primary"/>
                    <button string="Hủy" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Lock Date Action -->
    <record id="action_account_lock_date" model="ir.actions.act_window">
        <field name="name">Khóa Kỳ Kế toán</field>
        <field name="res_model">account.lock.date</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>

